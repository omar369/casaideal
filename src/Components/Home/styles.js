import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    root: {
        display:'flex',
        justifyContent: 'center',
    },
    welcomePaper: {
        margin: '2%',
        marginTop: '20%',
        padding: '5%',
        color: '#a9d',
        borderRadius: 10,
    },
    anunciosBtn: {
        borderColor: '#a9d',
        borderWidth: 2,
        color: '#a9d'
    }
  }));